{
	"name": "hunger",
	"version": "0.0.1",
	"engines": {
		"node": ">=0.12.0"
	},
	"dependencies": {
		"libxmljs": "^0.13.0",
		"restify": "^2.8.5"
	},
	"devDependencies": {
		"mocha": "^2.1.0"
	},
	"scripts": {
		"test": "mocha --require test/support/env --reporter spec --bail --check-leaks test/"
	}
}
